@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.Title = "Main Page";
}

@section body {
    
    <script>
        // Load the SDK Asynchronously
        (function (d) {
            var js, id = 'facebook-jssdk', ref = d.getElementsByTagName('script')[0];
            if (d.getElementById(id)) { return; }
            js = d.createElement('script'); js.id = id; js.async = true;
            js.src = "//connect.facebook.net/en_US/all.js";
            ref.parentNode.insertBefore(js, ref);
        }(document));

        window.fbAsyncInit = function () {
            FB.init({
                appId: '393153644124938', // App ID
                status: true, // check login status
                cookie: true, // enable cookies to allow the server to access the session
                xfbml: true  // parse XFBML
            });

            // Additional initialization code here
            FB.Event.subscribe('auth.authResponseChange', function (response) {
                if (response.status === 'connected') {
                    // the user is logged in and has authenticated your
                    // app, and response.authResponse supplies
                    // the user's ID, a valid access token, a signed
                    // request, and the time the access token 
                    var uid = response.authResponse.userID;
                    var accessToken = response.authResponse.accessToken;

                    console.log(accessToken);

                } else if (response.status === 'not_authorized') {
                    // the user is logged in to Facebook, 
                    // but has not authenticated your app
                } else {
                    // the user isn't logged in to Facebook.
                }
            });
        };

        function findAndAddFriend(userName) {
            FB.api({
                method: 'fql.query',
                query: 'SELECT uid FROM user WHERE username=' + userName,
            }, function (response) {
                alert(JSON.stringify(response)); //DEBUG PURPOSES ONLY!
            });
            addFriend(uid);
        }

        function findFriend(username) {
            FB.api({ method: 'friends.get' }, function (response) {
                alert(JSON.stringify(response)); //DEBUG PURPOSES ONLY!
            });
        }

        function messageFriend() {
            FB.ui({
                method: 'send',
                link: 'http://portalface.net/'
                //to: uid,
            }, function (response) {
                //alert(JSON.stringify(response)); //DEBUG PURPOSES ONLY!
            });
        }
        function addFriend( uid ) {
            
            FB.ui({
                method: 'friends.add',
                id: uid,
            }, function (response) {
                //alert(JSON.stringify(response)); //DEBUG PURPOSES ONLY!
            });
        }

        function getNewsFeed() {
            console.log("fuck me");
            FB.api('/me/feed',
                 function (response) {
                     //alert(JSON.stringify(response)); //DEBUG PURPOSES ONLY!
                     $("#feed").html(JSON.stringify(response));
                 });
        }
    </script>
    <!-- Look into using a more recent jQuery -->
    <!--<script type="text/javascript" src="http://jqueryjs.googlecode.com/files/jquery-1.2.6.min.js"></script>-->
    <link rel="stylesheet" href="http://code.jquery.com/ui/1.10.2/themes/smoothness/jquery-ui.css">
    <script src="http://code.jquery.com/jquery-1.9.1.js"></script>
    <script src="http://code.jquery.com/ui/1.10.2/jquery-ui.js"></script>
    
    @Scripts.Render( "~/Scripts/widget_functionality.js", "~/Scripts/LoadMainContent.js")
    
    
    <div id="columns">
        
        <ul id="column1" class="column">
            <li class="widget color-purple" >
                <div class="widget-head">
                    <h3>Facebook Actions</h3>
                </div>
                <div class="widget-content">
                    <h3>Welcome back @ViewBag.Name</h3>
                    <div id="fbactions">
                        <h4>Search Friends</h4>
                        <div>Friend's Name: <input type="text" /></div>
                        <h4>Message Friends</h4>
                        <div><button onclick="messageFriend()">Open Message Dialog</button></div>
                        <h4>Add Friends</h4>
                        <div><a onclick="addFriend( 'andresruiztorres' )"></a></div>
                        <h4>Add Photo</h4>
                        <div><a href="#">Add Photos</a></div>
                    </div>
                    <script>
                        $("#fbactions").accordion();
                    </script>
                </div>
            </li>
            <li class="widget color-yellow">  
                <div class="widget-head">
                    <h3>News Feed</h3>
                </div>
                <div id="newsfeed" class="widget-content">
                    <button onclick="getNewsFeed()">Load Feed</button>
                    <div id="feed">hi</div>
                </div>
            </li>
            
        </ul>

        <ul id="column2" class="column">
            <li class="widget color-blue">  
                <div class="widget-head">
                    <h3>Post to Facebook</h3>
                </div>
                
                <div class="widget-content" id="status">

                        
                </div>
            </li>
            <li class="widget color-orange">  
                <div class="widget-head">
                    <h3>Stock Watch</h3>
                </div>
                <!--<a href = "/Home/StockPage" class="widget-content-link"> -->
                    <div id="stocks">
                        <input id="ticker" name="ticker" type="text" value="Stock Symbol" /><button onclick="searchStock()">Search</button>
                        <div id="searchResults"></div>
                        <script>
                            function searchStock() {
                                var ticker = $("#ticker").val();
                                console.log(ticker);
                                $.get("/PortalFace/StockInfo", { stockSymbol: ticker})
                                    .done(function (data) {
                                        $("#searchResults").html(data);
                                        $("#ticker").val("");
                                    });
                            }
                        </script>
                    </div>
                <!--</a>-->
            </li>
        </ul>
        
        <ul id="column3" class="column">
            <li class="widget color-green">  
                <div class="widget-head">
                    <h3>Schedule</h3>
                </div>
                
                    <div id="events" >
                        <p>
                        <a href = "/Home/CalendarPage" class="widget-content-link">
                            <i class="icon-calendar"></i>  You have no events, take the day off!
                        </a>
                        </p>
                    </div>
            </li>
            <li class="widget color-red">  
                <div class="widget-head">
                    <h3>Global Chat</h3>
                </div>
                <div class="widget-content" id="chat">
                    <!-- Check out the LoadMainContent.js Script -->
                </div>
            </li>
            
        </ul>
        
    </div>
    
    
    

}
